apiVersion: v1
kind: Service
metadata:
  name: nelm-source-controller
  labels:
    control-plane: controller
spec:
  type: ClusterIP
  selector:
    app: nelm-source-controller
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
